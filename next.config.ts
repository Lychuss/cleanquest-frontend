import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["*.ngrok-free.app", "*.ngrok-free.dev", "10.0.0.2", "closes-quiet-protective-important.trycloudflare.com", "127.0.0.1"],
};

export default nextConfig;
